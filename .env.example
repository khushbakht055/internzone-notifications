# Option 1: Base64 encoded service account (recommended for deployment)
FIREBASE_SERVICE_ACCOUNT_BASE64=your_base64_encoded_service_account_here

# Option 2: Direct JSON (alternative)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# Server port
PORT=3000
